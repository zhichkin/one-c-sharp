<UserControl x:Class="Zhichkin.Hermes.UI.JoinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:Zhichkin.Hermes.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Orientation="Vertical" VerticalAlignment="Top">
        <TextBlock Text="{Binding JoinType}" FontWeight="Bold"/>
        <ui:TableExpressionView DataContext="{Binding Table}" Margin="15,0,0,0"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="ON" FontWeight="Bold" />
            <ui:BooleanExpressionView DataContext="{Binding Filter}" Margin="10,0,0,0"/>
        </StackPanel>
    </StackPanel>

</UserControl>
