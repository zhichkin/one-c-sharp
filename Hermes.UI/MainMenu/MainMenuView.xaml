<UserControl x:Class="Zhichkin.Hermes.UI.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Zhichkin.Hermes.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Menu Margin="5,0,5,0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <MenuItem Header="Hermes">
                <MenuItem Header="Add new QUERY" Command="{Binding AddNewQueryCommand}"/>
                <MenuItem Header="Add new SELECT statement" Command="{Binding AddNewSelectStatementCommand}"/>
                <MenuItem Header="Add TABLE to select statement" Command="{Binding AddTableToSelectStatementCommand}"/>
                <MenuItem Header="Change FROM orientation" Command="{Binding ChangeFromOrientationCommand}"/>
                <Separator/>
                <MenuItem Header="Build metadata tree" Command="{Binding BuildMetadataTreeCommand}"/>
            </MenuItem>
        </Menu>
    </Grid>
</UserControl>
