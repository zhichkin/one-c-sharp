<UserControl x:Class="Zhichkin.Hermes.UI.PropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:Zhichkin.Hermes.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Orientation="Horizontal">
        <TextBox Text="{Binding Alias}" MinWidth="30"/>
        <TextBlock Text="{Binding FullName}" Margin="5,0,0,0"/>
        <ComboBox ItemsSource="{Binding AvailablePropertiesForSelection}"
                  SelectedItem="{Binding PropertySelected, UpdateSourceTrigger=PropertyChanged}"
                  DisplayMemberPath="Name"/>
    </StackPanel>
    
</UserControl>
